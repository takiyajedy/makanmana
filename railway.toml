[build]
builder = "NIXPACKS"
buildCommand = "composer install --no-dev --optimize-autoloader && npm install && npm run build && php artisan migrate:refresh --seed --force"


# Command yang run sekali setiap kali deploy
releaseCommand = "php artisan migrate --seed --force"
